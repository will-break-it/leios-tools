# Vote diffusion arm: announce-then-request, echo-to-source false.
# Layer over a scenario config, e.g.
#   sim-cli <topology> -p parameters/linear-with-tx-references.yaml \
#     -p parameters/turbo.yaml -p parameters/vote-announce.yaml
vote-transport: "announce-then-request"
vote-transport-echo-to-source: false
