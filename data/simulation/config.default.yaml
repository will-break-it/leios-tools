# yaml-language-server: $schema=./config.schema.json

################################################################################
# Simulation Configuration File
################################################################################
#
# This file contains the default configuration for running Leios simulations in
# the Haskell simulation (`simulation/`) and the Rust simulation (`sim-rs/`).
#
################################################################################
# Simulation Configuration
################################################################################

relay-strategy: "request-from-first"
tcp-congestion-control: true
multiplex-mini-protocols: true
simulate-transactions: true
treat-blocks-as-full: false
cleanup-policies: ["cleanup-expired-vote"]
timestamp-resolution-ms: 0.000001

################################################################################
# Leios Protocol Configuration
################################################################################

leios-variant: short
leios-stage-length-slots: 20
leios-stage-active-voting-slots: 1
leios-vote-send-recv-stages: false
leios-late-ib-inclusion: true
leios-header-diffusion-time-ms: 1000.0
leios-ib-generation-time-ms: 130.0
leios-mempool-sampling-strategy: ordered-by-id
leios-mempool-aggressive-pruning: false
leios-mempool-size-bytes: null
leios-tx-generated-backlog-max-size: null
leios-tx-peer-backlog-max-size: null
# TODO: revise default
praos-chain-quality: 40
praos-fallback-enabled: true

linear-vote-stage-length-slots: 5
linear-diffuse-stage-length-slots: 5
# Which conditions must be met before a node will send an EB to peers?
# - "eb-received": the EB must only go through basic validation
# - "txs-received": all TXs in the EB must have been received
# - "fully-valid": the EB must be fully validated
linear-eb-propagation-criteria: eb-received
# Maximum age (in slots) before a transaction can be pruned from node memory.
# When set, transactions older than this that are no longer in the mempool
# will be removed to limit memory growth during long simulations.
# Suggested value: linear-vote-stage-length-slots + linear-diffuse-stage-length-slots
#   + ceil(leios-header-diffusion-time-ms / 1000) * 3 + buffer
# With defaults: 5 + 5 + 3 + 10 = 23
linear-tx-max-age-slots: null
store-txs-as-references: false

################################################################################
# Transaction Configuration
################################################################################

tx-generation-distribution:
  distribution: exp
  lambda: 0.85
  scale: 1000.0
tx-size-bytes-distribution:
  distribution: log-normal
  mu: 6.833
  sigma: 1.127
tx-overcollateralization-factor-distribution:
  distribution: constant
  value: 0
tx-validation-cpu-time-ms: 1.5
tx-validation-cpu-time-ms-per-byte: 0
tx-max-size-bytes: 16384
tx-conflict-fraction: 0
tx-batch-window-ms: null

################################################################################
# Ranking Block Configuration
################################################################################

# 1/leios-stage-length-slots, targeting one RB per pipeline.
# Also 20s is current rate of praos blocks.
rb-generation-probability: 5.0e-2
# Eng. team targets 1kB as worst case upper bound.
# Actual size fairly close.
rb-head-size-bytes: 1024
rb-body-max-size-bytes: 90112
# Note: certificate generation/validation is not included in the
# timings here, see cert-* fields.
rb-generation-cpu-time-ms: 1.0
rb-head-validation-cpu-time-ms: 1.0

# On average, no Txs directly embedded in blocks.
rb-body-legacy-praos-payload-avg-size-bytes: 0
rb-body-legacy-praos-payload-validation-cpu-time-ms-constant: 50.0
# the -per-byte component is meant to be using size as a (bad)
# proxy for the complexity of the Txs included.
rb-body-legacy-praos-payload-validation-cpu-time-ms-per-byte: 0.0005
# Ledger-apply cost for an RB (UTXO mutation, after body-validation
# has already checked signatures / structure).  Scales by tx count
# because a real ledger walks every input + output.  Minimal default
# — studies of the fork-rate-vs-apply-latency curve dial these up.
rb-apply-cpu-time-ms: 0.5
rb-apply-cpu-time-ms-per-tx: 0.05

################################################################################
# Input Block Configuration
################################################################################

ib-generation-probability: 5.0
ib-shards: 50
ib-shard-period-length-slots: 5
ib-shard-group-count: 25

# ProducerId  32
# SlotNo      64
# VRF proof   80
# Body hash   32
# RB Ref      32
# Signature   64
# Total       304
#
# NOTE: using a KES Signature (like for Praos headers)
#       would instead more than double the total to 668.
#       And even 828 including Op Cert.
ib-head-size-bytes: 304
# 98KB to optimize for 3 TCP round trips
ib-body-avg-size-bytes: 98304
ib-body-max-size-bytes: 327680
# Here we also use praos blocks as ballpark estimate.
# Sec 2.3 Forging, of the benchmark cluster report, lists
#   * Slot start to announced: 0.12975s
ib-generation-cpu-time-ms: 130.0
ib-head-validation-cpu-time-ms: 1.0
ib-body-validation-cpu-time-ms-constant: 50.0
ib-body-validation-cpu-time-ms-per-byte: 0.0005
ib-diffusion-strategy: "freshest-first"

# Haskell prototype relay mini-protocol parameters.
ib-diffusion-max-bodies-to-request: 1
ib-diffusion-max-headers-to-request: 100
ib-diffusion-max-window-size: 100

################################################################################
# Endorsement Block Configuration
################################################################################

# We want one per pipeline, but not too many.
eb-generation-probability: 1.5
# ProducerId  32
# SlotNo      64
# VRF proof   80
# Signature   64
# Total       240
#
# See Note about signatures on ib-head-size-bytes.
eb-size-bytes-constant: 240
# IB hash
eb-size-bytes-per-ib: 32
# Collecting the IBs to reference and cryptography are the main tasks.
# A comparable task is maybe mempool snapshotting.
# Sec 2.3 Forging, of the benchmark cluster report, lists
#   * Mempool snapshotting: 0.07252s
# 75ms then seems a generous estimate for eb generation.
eb-generation-cpu-time-ms: 75.0
# Validating signature and vrf proof, as in other headers.
eb-validation-cpu-time-ms: 1.0
# For linear leios: "fast" incomplete validation of the EB
eb-header-validation-cpu-time-ms: 50.0
# For linear leios: complete validation of the EB
eb-body-validation-cpu-time-ms-constant: 50.0
eb-body-validation-cpu-time-ms-per-byte: 0.0005
# Ledger-apply cost for an EB closure — the EB's referenced txs land
# on-chain via the certifying RB's endorsement.  Parallel to
# rb-apply-cpu-time-ms; gates mempool pruning of the EB's tx set.
eb-apply-cpu-time-ms: 0.5
eb-apply-cpu-time-ms-per-tx: 0.05
# For stracciatella: when creating a new EB, have it reference transactions from a previous EB
eb-include-txs-from-previous-stage: false

eb-diffusion-strategy: "peer-order"

# Haskell prototype relay mini-protocol parameters.
eb-diffusion-max-bodies-to-request: 1
eb-diffusion-max-headers-to-request: 100
eb-diffusion-max-window-size: 100

# The maximum age of EBs included in RBs.
# A an EB from slot `s` can only be included in RBs
# up to slot `s+eb-max-age-slots`.
# In short leios we expect votes to diffuse within 3 stages lengths of
# EB generation, we allow for 2 more stage lengths to account for
# variance in the interval within RBs.
eb-max-age-slots: 100

# The maximum age of EBs to be relayed.
# An EB from slot `s` will only be relayed
# up to slot `s+eb-max-age-for-relay-slots`.
eb-max-age-for-relay-slots: 40

# The maximum size of transactions (in bytes) which an EB can reference.
# Only relevant when running with the "full-without-ibs" variant.
eb-referenced-txs-max-size-bytes: 16384000

# For Linear Leios
eb-body-avg-size-bytes: 2500000

################################################################################
# Vote Configuration
################################################################################

# Cryptography related values taken from [vote-spec](crypto-benchmarks.rs/Specification.md)
# with separate persistent and non-persistent voter parameters.

# vote-spec#Committee and quorum size
#
# Note: total vote-generation-probability is the sum of persistent and
# non-persistent probabilities.
persistent-vote-generation-probability: 400.0
non-persistent-vote-generation-probability: 100.0
# vote-spec#"Committee and quorum size"
# Fraction of expected total weight required for quorum (CIP-0164
# default is 0.75; legacy sim runs used 0.6 = old absolute 300 / 500).
# The absolute threshold is derived per committee mode:
#   wfa-ls             → quorum-weight-fraction × (PV + NPV)
#   everyone           → quorum-weight-fraction × N nodes
#   top-stake-fraction → quorum-weight-fraction × total active stake
# Replaces the old `vote-threshold` knob; see CIP-164 PR #1196.
quorum-weight-fraction: 0.6
# vote-spec#"Generate vote"
persistent-vote-generation-cpu-time-ms: 135e-3
non-persistent-vote-generation-cpu-time-ms: 280e-3
# No benchmark yet.
vote-generation-cpu-time-ms-per-ib: 0
# No benchmark yet.
vote-generation-cpu-time-ms-per-tx: 0
# vote-spec#"Verify vote"
persistent-vote-validation-cpu-time-ms: 670e-3
non-persistent-vote-validation-cpu-time-ms: 1.4
# The `Vote` structure counted in the -per-eb already identifies slot
# (in Eid) and voter. We can assume a vote bundle is all for the same
# voter and slot, so for non-persistent voters we could factor their
# PoolKeyHash (28bytes) here, but that is for 20% of cases.
# More relevant if EB generation is very high.
vote-bundle-size-bytes-constant: 0
# vote-spec#Votes
persistent-vote-bundle-size-bytes-per-eb: 90
non-persistent-vote-bundle-size-bytes-per-eb: 164

# Grandfathered weighted averages for Haskell sim compatibility.
# These are computed as 0.8*persistent + 0.2*non-persistent.
vote-generation-probability: 500.0
vote-generation-cpu-time-ms-constant: 164e-3
vote-validation-cpu-time-ms: 816e-3
vote-bundle-size-bytes-per-eb: 105
# Absolute quorum threshold consumed by the Haskell sim (no committee
# modes there). Mirrors quorum-weight-fraction × vote-generation-probability
# = 0.6 × 500 = 300. Keep in sync with quorum-weight-fraction above.
vote-threshold: 300

# Committee selection algorithm: "wfa-ls" (default), "everyone",
# "top-stake-fraction" or "top-stake-seats".
# The same `quorum-weight-fraction` knob applies to all modes; the
# absolute threshold is derived from it via the mode-specific
# denominator (see above).
committee-selection-algorithm: "wfa-ls"
# Fraction of total stake eligible to vote when using "top-stake-fraction".
committee-stake-fraction-threshold: 0.95
# Committee size in seats when using "top-stake-seats". CIP-0164 governs the
# committee by seat count and recommends 900. The top `committee-seat-count`
# stake-holding pools are seated, ties broken by pool identifier ascending,
# and each seat votes with weight 1. A topology with fewer stake-holding
# pools than seats requested seats every pool it has, and the quorum
# denominator shrinks to match; that is the expected case, not an error.
committee-seat-count: 900

# Request ordering for vote relay.  Read by the Haskell simulator only; this
# simulator never looks at it.  Accepted values are "peer-order",
# "freshest-first" and "oldest-first".  Nothing to do with `vote-transport`
# below, which is the knob this simulator does read -- the two share this file
# and nothing else.
vote-diffusion-strategy: "peer-order"

# How a vote bundle reaches a peer in this simulator.
#
#   announce-then-request  announce the bundle id, wait for the peer to ask,
#                          then send the body.  Each body crosses the network
#                          exactly once per node, so there are no duplicates.
#                          Historic behaviour, and the default so that earlier
#                          studies stay reproducible.
#   push                   send the body straight to peers, forwarding a bundle
#                          only the first time it is seen.  What both Leios node
#                          implementations do.  Traffic is bounded by links
#                          rather than by nodes.  A copy arriving while another
#                          is still being validated is dropped before it costs
#                          anything.
#   push-late-dedupe       as push, but suppression applies only once a bundle
#                          is fully held, so copies arriving during the
#                          validation window are each validated.  This is the
#                          Haskell node's ordering, which verifies before
#                          inserting into its seen-set.
#   push-no-dedupe         forward on every arrival, including bundles already
#                          held.  Not a run arm: forwarding on every arrival has
#                          no damping, so on any topology with cycles this is a
#                          broadcast storm that does not terminate.  Kept as the
#                          evidence that suppression is load-bearing.
#
# Those four are the only accepted values.  "peer-order", "freshest-first" and
# "oldest-first" belong to vote-diffusion-strategy above; they are not aliases
# of anything here, and setting one of them here fails to load the config.
vote-transport: "announce-then-request"

# Push a vote body back to the peer it arrived from.  Pure waste, and off here,
# but the Haskell node's notify server has no per-peer provenance and so does
# exactly this.  The option exists to measure that gap.  It applies to the push
# transports only: announce-then-request has no notify server to model, and it
# is the published baseline, so the flag leaves it alone.
vote-transport-echo-to-source: false

# Push a vote body to at most this many peers instead of every consumer.
# Unset (the default) means every consumer, which is what both node
# implementations do.
#
# This is a proposal, not a CIP-0164 rule.  The specification caps how many
# *distinct* votes a node relays per committee member per slot, which the
# bundle key already enforces here; nothing in it bounds how many *copies* of
# one vote cross the network, and the copies are what cost verification time.
# Applies to the push transports only: announce-then-request sends a body only
# when a peer asks for it and so produces no copies to cut.
vote-push-fanout: null

# Haskell prototype relay mini-protocol parameters.  Not read by this simulator;
# kept for parity with the Haskell simulator's configuration.
vote-diffusion-max-bodies-to-request: 1
vote-diffusion-max-headers-to-request: 100
vote-diffusion-max-window-size: 100

################################################################################
# Certificate Configuration
################################################################################

# vote-spec - certificate size plot.
# Realistic stake distributions need about 7 kilobytes for the certificate.
cert-size-bytes-constant: 7168
cert-size-bytes-per-node: 0

# For certificate timings we have bulk figures for realistic scenarios,
# so we do not attempt to give -per-node (i.e. per-voter) timings.
#
# vote-spec#"Generate certificate"
cert-generation-cpu-time-ms-constant: 90.0
cert-generation-cpu-time-ms-per-node: 0
# vote-spec#"Verify certificate"
cert-validation-cpu-time-ms-constant: 130.0
cert-validation-cpu-time-ms-per-node: 0

################################################################################
# Consensus Behaviours (shared-consensus adapter only)
################################################################################

# Per-node adversarial / experimental behaviours wired into the
# shared-consensus engine.  Each entry pairs a `BehaviourSpec` (kind
# = honest | composite | rb-header-equivocator | lazy-voter) with a
# `BehaviourSelection` (kind = all | nodes | stake-random |
# stake-ordered | stake-fraction) picking which nodes run it.
# Overlapping selections compose; resolution is deterministic for a
# given `seed`.  Empty (default) means every node runs `honest`.
# Ignored by engines other than `shared-consensus`.
consensus-behaviours: []
